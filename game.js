(() => {
'use strict';

const $=id=>document.getElementById(id);
const canvas=$('game'),ctx=canvas.getContext('2d');
let W=innerWidth,H=innerHeight,DPR=Math.min(devicePixelRatio||1,2),last=performance.now();
function resize(){const vv=visualViewport;W=Math.max(1,Math.round(vv?.width||innerWidth));H=Math.max(1,Math.round(vv?.height||innerHeight));DPR=Math.min(devicePixelRatio||1,2);canvas.width=W*DPR;canvas.height=H*DPR;canvas.style.width=W+'px';canvas.style.height=H+'px';ctx.setTransform(DPR,0,0,DPR,0,0)}
addEventListener('resize',resize);resize();

const UI={menu:$('menu'),how:$('how'),pause:$('pause'),between:$('between'),gameover:$('gameover'),hud:$('hud'),wave:$('waveNum'),score:$('scoreNum'),left:$('zombiesLeft'),hp:$('hpFill'),hpText:$('hpText'),weapon:$('weaponName'),ammo:$('ammoText'),grenades:$('grenadeText'),interaction:$('interaction'),toast:$('toast'),damage:$('damageVignette'),cross:$('crosshair'),slots:$('weaponSlots'),reorder:$('slotReorder'),reorderGrid:$('slotReorderGrid'),shop:$('betweenWeaponShop')};
const TAU=Math.PI*2,rand=(a,b)=>a+Math.random()*(b-a),clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),lerp=(a,b,t)=>a+(b-a)*t,dist=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y);
function circleRect(cx,cy,r,o){const nx=clamp(cx,o.x,o.x+o.w),ny=clamp(cy,o.y,o.y+o.h);return (cx-nx)**2+(cy-ny)**2<r*r}
function resolveCircleRect(e,o,r=e.r){let nx=clamp(e.x,o.x,o.x+o.w),ny=clamp(e.y,o.y,o.y+o.h),dx=e.x-nx,dy=e.y-ny,d=Math.hypot(dx,dy);if(d<r&&d>0){const p=r-d;e.x+=dx/d*p;e.y+=dy/d*p;return true}return false}
function lineRect(x1,y1,x2,y2,o){const steps=Math.max(1,Math.ceil(Math.hypot(x2-x1,y2-y1)/12));for(let i=0;i<=steps;i++){const t=i/steps,x=lerp(x1,x2,t),y=lerp(y1,y2,t);if(x>=o.x&&x<=o.x+o.w&&y>=o.y&&y<=o.y+o.h)return true}return false}

const {maps,makeMap}=window.DeadSectorMaps;
const memory={};
function safeGet(k,d='{}'){try{const v=localStorage.getItem(k);return v===null?d:v}catch{return Object.prototype.hasOwnProperty.call(memory,k)?memory[k]:d}}
function safeSet(k,v){memory[k]=v;try{localStorage.setItem(k,v)}catch{}}
function safeJSON(k){try{return JSON.parse(safeGet(k,'{}'))||{}}catch{return {}}}
const saved=safeJSON('deadSectorSave');saved.bestWave=saved.bestWave||1;saved.highScore=saved.highScore||0;saved.unlock=saved.unlock||1;if(saved.unlock>5)saved.unlock=saved.unlock>=30?5:saved.unlock>=22?4:saved.unlock>=15?3:saved.unlock>=8?2:1;
const save=()=>safeSet('deadSectorSave',JSON.stringify(saved));
let selectedMap=0;
function renderMapList(){const el=$('mapList');el.innerHTML='';maps.forEach((m,i)=>{const unlocked=saved.unlock>=m.unlock,b=document.createElement('button');b.className='mapBtn '+(selectedMap===i?'active ':'')+(unlocked?'':'lockedLevel');b.disabled=!unlocked;b.style.opacity=unlocked?'1':'.28';if(!unlocked)b.dataset.lockhint=`Complete Level ${i} to unlock`;b.innerHTML=`<div><div class="mapLevel">LEVEL ${i+1}</div><div class="mapSite">${m.name}</div><div class="stars">${'★'.repeat(m.stars)}</div><div class="mapMeta">${m.desc}</div></div>`;b.onclick=()=>{if(unlocked){selectedMap=i;renderMapList()}};el.appendChild(b)});$('records').innerHTML=`Best wave: <b>${saved.bestWave}</b><br>High score: <b>${saved.highScore.toLocaleString()}</b><br>Unlocked levels: <b>${Math.min(saved.unlock,5)}/5</b>`}
renderMapList();

const DSMobile=window.DeadSectorMobile||{touchMode:false,initDeviceUI(){},syncOrientation(){},installBrowserGuards(){},setupStick(){}};
const touchMode=DSMobile.touchMode;DSMobile.initDeviceUI();const syncOrientation=()=>DSMobile.syncOrientation(resize);addEventListener('orientationchange',()=>setTimeout(syncOrientation,150));syncOrientation();
const ensureAudio=()=>window.DeadSectorAudio?.ensure?.();const sfx=(...a)=>window.DeadSectorAudio?.sfx?.(...a);

let state='menu',keys={},mouse={x:W/2,y:H/2,down:false,worldX:0,worldY:0},cam={x:0,y:0,shake:0},game=null,reorderOpen=false;
const defaultBinds={up:'KeyW',down:'KeyS',left:'KeyA',right:'KeyD',reload:'KeyR',grenade:'KeyG',interact:'KeyF',slot1:'Digit1',slot2:'Digit2',slot3:'Digit3',pause:'Escape'};
let binds={...defaultBinds,...safeJSON('deadSectorBinds')},rebinding=null;
const bindLabels={up:'Move Up',down:'Move Down',left:'Move Left',right:'Move Right',reload:'Reload',grenade:'Throw Grenade',interact:'Interact / Repair',slot1:'Weapon Slot 1',slot2:'Weapon Slot 2',slot3:'Weapon Slot 3',pause:'Pause'};
const prettyKey=c=>c.replace('Key','').replace('Digit','').replace('Escape','Esc');
function saveBinds(){safeSet('deadSectorBinds',JSON.stringify(binds))}
function renderControls(){const el=$('controlList');el.innerHTML='';for(const [a,l] of Object.entries(bindLabels)){const n=document.createElement('div');n.className='controlName';n.textContent=l;const b=document.createElement('button');b.className='bindBtn'+(rebinding===a?' waiting':'');b.textContent=rebinding===a?'PRESS A KEY':prettyKey(binds[a]);b.onclick=()=>{rebinding=a;renderControls()};el.append(n,b)}for(const [n,v] of [['Aim','Mouse'],['Fire','Left Mouse'],['Rearrange Weapons','Tab']]){const a=document.createElement('div');a.className='controlName';a.textContent=n;const b=document.createElement('div');b.className='fixedBind';b.textContent=v;el.append(a,b)}}
function showPausePane(which){const c=which==='controls';$('pauseMainPane').classList.toggle('hidden',c);$('pauseControlsPane').classList.toggle('hidden',!c);if(c)renderControls();else rebinding=null}

const weaponDefs={
 pistol:{name:'M9 Sidearm',damage:38,rate:.26,mag:12,reload:1.08,spread:.035,pellets:1,range:720,sound:'shot',color:'#e9eef5',cost:0},
 smg:{name:'VX-9 SMG',damage:24,rate:.085,mag:32,reload:1.35,spread:.07,pellets:1,range:650,sound:'smg',color:'#55d7ff',cost:1700},
 shotgun:{name:'M12 Breacher',damage:20,rate:.62,mag:8,reload:1.65,spread:.19,pellets:7,range:480,sound:'rifle',color:'#f2c14e',cost:2200},
 rifle:{name:'AR-47',damage:48,rate:.145,mag:24,reload:1.50,spread:.035,pellets:1,range:880,sound:'rifle',color:'#ff8f66',cost:3000}
};

function newWeapon(id){return{id,ammo:weaponDefs[id].mag}}
function startGame(){ensureAudio();resize();const map=makeMap(selectedMap);game={map,wave:0,score:0,kills:0,shots:0,hits:0,time:0,enemies:[],bullets:[],particles:[],pickups:[],grenades:[],corpses:[],waveQueue:0,spawnTimer:0,waveActive:false,healDropChance:.05,power:{double:0,rapid:0},player:{x:map.size[0]/2,y:map.size[1]/2,r:18,hp:100,maxHp:100,speed:230,weapons:[newWeapon('pistol')],slot:0,grenades:0,fireCd:0,reloading:0,invuln:0,damageMult:1,fireMult:1,moveMult:1,healPower:40}};state='playing';reorderOpen=false;UI.reorder.classList.add('hidden');document.body.classList.add('game-live');UI.menu.classList.add('hidden');UI.gameover.classList.add('hidden');UI.pause.classList.add('hidden');UI.between.classList.add('hidden');UI.hud.style.display='block';UI.cross.style.display=touchMode?'none':'block';updateWeaponSlots();nextWave()}
function nextWave(){game.wave++;game.waveActive=true;game.waveQueue=6+game.wave*3+Math.floor(game.wave**1.22)+(game.wave%5===0?1:0);game.spawnTimer=.45;toast(`WAVE ${game.wave}`);updateHUD()}

function enemyStats(type){const w=game.wave,d=game.map.difficulty||1,scale=s=>Math.round(s*d);if(type==='runner')return{hp:scale(55+w*6),speed:(108+w*1.6)*(1+(d-1)*.34),dmg:12,r:14,color:'#d96354',score:130};if(type==='brute')return{hp:scale(285+w*24),speed:(54+w*.75)*(1+(d-1)*.28),dmg:24,r:25,color:'#8f4d3b',score:470};if(type==='elite')return{hp:scale(980+w*80),speed:(74+w*.7)*(1+(d-1)*.3),dmg:32,r:31,color:'#b44bdb',score:1650};return{hp:scale(100+w*10),speed:(72+w*1.05)*(1+(d-1)*.30),dmg:10,r:18,color:'#769057',score:105}}
function spawnEnemy(){const s=game.map.spawns[Math.floor(Math.random()*game.map.spawns.length)],r=Math.random();let type=r<Math.min(.14+game.wave*.008,.30)?'runner':r>.92&&game.wave>3?'brute':'walker';if(game.wave%5===0&&game.waveQueue===1)type='elite';const st=enemyStats(type);game.enemies.push({x:s.x+rand(-25,25),y:s.y+rand(-25,25),...st,maxHp:st.hp,attackCd:rand(0,.3),hitFlash:0,path:[],pathTimer:0})}

function shoot(){if(state!=='playing'||reorderOpen)return;const p=game.player,w=p.weapons[p.slot],d=weaponDefs[w.id];if(p.reloading>0||p.fireCd>0)return;if(w.ammo<=0){reload();return}w.ammo--;game.shots++;p.fireCd=d.rate/(p.fireMult*(game.power.rapid>0?1.55:1));const a=Math.atan2(mouse.worldY-p.y,mouse.worldX-p.x);for(let k=0;k<d.pellets;k++){const aa=a+rand(-d.spread,d.spread),range=d.range*rand(.88,1.02);let ex=p.x+Math.cos(aa)*range,ey=p.y+Math.sin(aa)*range;for(const o of game.map.obs)if(lineRect(p.x,p.y,ex,ey,o)){const steps=Math.ceil(range/8);for(let i=1;i<=steps;i++){const t=i/steps,x=lerp(p.x,ex,t),y=lerp(p.y,ey,t);if(x>=o.x&&x<=o.x+o.w&&y>=o.y&&y<=o.y+o.h){ex=x;ey=y;break}}break}let best=null,bd=Infinity;for(const e of game.enemies){const vx=ex-p.x,vy=ey-p.y,wx=e.x-p.x,wy=e.y-p.y,c1=vx*wx+vy*wy,c2=vx*vx+vy*vy,t=clamp(c1/c2,0,1),px=p.x+vx*t,py=p.y+vy*t,dd=Math.hypot(e.x-px,e.y-py);if(dd<e.r+4){const along=Math.hypot(px-p.x,py-p.y);if(along<bd){bd=along;best=e;ex=px;ey=py}}}if(best){best.hp-=d.damage*p.damageMult;best.hitFlash=.08;game.hits++;burst(ex,ey,d.color,4,90);if(best.hp<=0)killEnemy(best)}game.bullets.push({x1:p.x,y1:p.y,x2:ex,y2:ey,life:.055,color:d.color})}cam.shake=Math.max(cam.shake,d.pellets>1?4:2);sfx(d.sound,.055);updateHUD()}
function reload(){const p=game.player,w=p.weapons[p.slot],d=weaponDefs[w.id];if(p.reloading>0||w.ammo>=d.mag)return;p.reloading=d.reload;sfx('reload',.035);toast('RELOADING')}
function finishReload(){const p=game.player,w=p.weapons[p.slot];w.ammo=weaponDefs[w.id].mag;updateHUD()}
function throwGrenade(){if(state!=='playing'||reorderOpen)return;const p=game.player;if(p.grenades<=0)return;p.grenades--;const a=Math.atan2(mouse.worldY-p.y,mouse.worldX-p.x);game.grenades.push({x:p.x,y:p.y,vx:Math.cos(a)*520,vy:Math.sin(a)*520,t:1.15,r:8});toast('GRENADE OUT');updateHUD()}
function explode(g){sfx('boom',.16);cam.shake=12;burst(g.x,g.y,'#ffb347',36,360);for(const e of [...game.enemies]){const d=Math.hypot(e.x-g.x,e.y-g.y);if(d<190){e.hp-=Math.max(0,280*(1-d/230))*game.player.damageMult;if(e.hp<=0)killEnemy(e)}}}
function killEnemy(e){const i=game.enemies.indexOf(e);if(i<0)return;game.enemies.splice(i,1);game.score+=Math.round(e.score*(game.power.double>0?2:1));game.kills++;game.corpses.push({x:e.x,y:e.y,r:e.r,a:Math.random()*TAU,life:8,color:e.color});const roll=Math.random();if(roll<game.healDropChance)game.pickups.push({x:e.x,y:e.y,r:14,type:'heal',life:14,bob:0});else if(roll<game.healDropChance+.02)game.pickups.push({x:e.x,y:e.y,r:14,type:'grenade',life:14,bob:0});else if(roll<game.healDropChance+.10)game.pickups.push({x:e.x,y:e.y,r:13,type:Math.random()<.5?'double':'rapid',life:12,bob:0});burst(e.x,e.y,e.color,10,140);updateHUD()}
function pickup(pu){const p=game.player;if(pu.type==='heal'){const a=Math.min(p.healPower,p.maxHp-p.hp);p.hp=Math.min(p.maxHp,p.hp+p.healPower);toast(a>0?`+${Math.ceil(a)} HEALTH`:'HEALTH FULL')}else if(pu.type==='grenade'){p.grenades=Math.min(5,p.grenades+1);toast('+1 GRENADE')}else if(pu.type==='double'){game.power.double=12;toast('DOUBLE POINTS — 12s')}else if(pu.type==='rapid'){game.power.rapid=12;toast('OVERDRIVE — 12s')}sfx('pickup',.05);game.pickups.splice(game.pickups.indexOf(pu),1);updateHUD()}
function burst(x,y,color,n=8,speed=100){for(let i=0;i<n;i++){const a=Math.random()*TAU,s=rand(speed*.3,speed);game.particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:rand(.18,.55),max:.55,size:rand(2,5),color})}}
function toast(t){const d=document.createElement('div');d.className='toast';d.textContent=t;UI.toast.appendChild(d);setTimeout(()=>d.remove(),2200)}
function damagePlayer(a){const p=game.player;if(p.invuln>0)return;p.invuln=.2;p.hp-=a;UI.damage.style.opacity=.72;setTimeout(()=>UI.damage.style.opacity=0,110);cam.shake=7;sfx('hurt',.08);if(p.hp<=0)endGame();updateHUD()}

function interact(){if(!game)return;const p=game.player;let best=null,bd=75;for(const b of game.map.barr){const d=Math.hypot(p.x-b.x-b.w/2,p.y-b.y-b.h/2);if(d<bd&&b.hp>0&&b.hp<b.maxHp){best=b;bd=d}}if(best){if(game.score>=250){game.score-=250;best.hp=Math.min(best.maxHp,best.hp+160);toast('BARRICADE REPAIRED');sfx('buy',.05)}else toast('NOT ENOUGH SCORE');updateHUD()}}
function updateInteraction(){if(!game)return;const p=game.player;let txt='',bd=80;for(const b of game.map.barr){const d=Math.hypot(p.x-b.x-b.w/2,p.y-b.y-b.h/2);if(d<bd&&b.hp>0&&b.hp<b.maxHp){txt=`[${prettyKey(binds.interact)}] REPAIR BARRICADE — 250`;bd=d}}UI.interaction.textContent=txt;UI.interaction.style.opacity=txt?1:0}

function renderBetweenShop(){if(!UI.shop)return;UI.shop.innerHTML='';for(const id of ['smg','shotgun','rifle']){const d=weaponDefs[id],owned=game.player.weapons.some(w=>w.id===id),row=document.createElement('div');row.className='shopChoice';row.innerHTML=`<div><b>${d.name}</b><br><span>${owned?'OWNED':d.cost.toLocaleString()+' SCORE'}</span></div><button ${owned?'disabled':''}>${owned?'OWNED':'BUY'}</button>`;row.querySelector('button').onclick=()=>{if(game.player.weapons.length>=3){toast('3 WEAPON LIMIT');return}if(game.score<d.cost){toast('NOT ENOUGH SCORE');return}game.score-=d.cost;game.player.weapons.push(newWeapon(id));game.player.slot=game.player.weapons.length-1;sfx('buy',.07);updateWeaponSlots();updateHUD();renderBetweenShop()};UI.shop.appendChild(row)}}
function chooseUpgrades(){const pool=[
 {t:'Stopping Power',d:'+20% weapon damage.',f:()=>game.player.damageMult*=1.2},
 {t:'Fleet Foot',d:'+12% movement speed.',f:()=>game.player.moveMult*=1.12},
 {t:'Fast Hands',d:'+16% fire rate.',f:()=>game.player.fireMult*=1.16},
 {t:'Juggernaut',d:'+25 max health. Restore 25.',f:()=>{game.player.maxHp+=25;game.player.hp=Math.min(game.player.maxHp,game.player.hp+25)}},
 {t:'Field Medicine',d:'+3% healing drops. Max 20%.',ok:()=>game.healDropChance<.20,f:()=>game.healDropChance=Math.min(.20,game.healDropChance+.03)},
 {t:'Potent Aid',d:'Healing pickups restore +15.',f:()=>game.player.healPower+=15},
 {t:'Emergency Care',d:'Restore 45 health now.',f:()=>game.player.hp=Math.min(game.player.maxHp,game.player.hp+45)},
 {t:'Quick Step',d:'+8% movement and fire rate.',f:()=>{game.player.moveMult*=1.08;game.player.fireMult*=1.08}},
 {t:'Heavy Rounds',d:'+12% damage and +10 max health.',f:()=>{game.player.damageMult*=1.12;game.player.maxHp+=10;game.player.hp+=10}},
 {t:'Grenadier',d:'Gain 2 grenades.',f:()=>game.player.grenades=Math.min(5,game.player.grenades+2)}
].filter(u=>!u.ok||u.ok()).sort(()=>Math.random()-.5).slice(0,3);const grid=$('upgradeGrid');grid.innerHTML='';for(const u of pool){const d=document.createElement('div');d.className='upgrade';d.innerHTML=`<h4>${u.t}</h4><p>${u.d}</p><button>SELECT</button>`;d.querySelector('button').onclick=()=>{u.f();UI.between.classList.add('hidden');state='playing';nextWave();updateHUD()};grid.appendChild(d)}renderBetweenShop();state='between';UI.between.classList.remove('hidden');document.body.classList.add('game-live')}
function endGame(){state='gameover';reorderOpen=false;UI.reorder.classList.add('hidden');UI.hud.style.display='none';UI.cross.style.display='none';saved.bestWave=Math.max(saved.bestWave,game.wave);saved.highScore=Math.max(saved.highScore,game.score);if(game.wave>=8&&selectedMap<maps.length-1)saved.unlock=Math.max(saved.unlock,selectedMap+2);save();renderMapList();$('endStats').innerHTML=`<div class="stat"><b>${game.wave}</b><span>Wave</span></div><div class="stat"><b>${Math.floor(game.score).toLocaleString()}</b><span>Score</span></div><div class="stat"><b>${game.kills}</b><span>Kills</span></div><div class="stat"><b>${game.shots?Math.round(game.hits/game.shots*100):0}%</b><span>Accuracy</span></div>`;UI.gameover.classList.remove('hidden')}

function updateWeaponSlots(){if(!game)return;document.querySelectorAll('.weaponSlot').forEach((el,i)=>{const w=game.player.weapons[i];el.classList.toggle('active',i===game.player.slot);el.querySelector('.slotWeapon').textContent=w?weaponDefs[w.id].name:'EMPTY'});if(reorderOpen)renderReorder()}
function renderReorder(){UI.reorderGrid.innerHTML='';for(let i=0;i<3;i++){const slot=document.createElement('div');slot.className='dragSlot';slot.dataset.slot=i;slot.innerHTML=`<div class="dragSlotLabel">SLOT ${i+1}</div>`;const w=game.player.weapons[i];if(w){const card=document.createElement('div');card.className='dragWeapon';card.draggable=true;card.dataset.slot=i;card.textContent=weaponDefs[w.id].name;card.addEventListener('dragstart',e=>e.dataTransfer.setData('text/plain',String(i)));slot.appendChild(card)}slot.addEventListener('dragover',e=>{e.preventDefault();slot.classList.add('dragOver')});slot.addEventListener('dragleave',()=>slot.classList.remove('dragOver'));slot.addEventListener('drop',e=>{e.preventDefault();slot.classList.remove('dragOver');const from=Number(e.dataTransfer.getData('text/plain')),to=i;if(!Number.isInteger(from)||from===to||!game.player.weapons[from])return;const ws=game.player.weapons;if(to>=ws.length){const moved=ws.splice(from,1)[0];ws.splice(Math.min(to,ws.length),0,moved)}else [ws[from],ws[to]]=[ws[to],ws[from]];game.player.slot=Math.min(game.player.slot,ws.length-1);updateWeaponSlots();updateHUD()});UI.reorderGrid.appendChild(slot)}}
function toggleReorder(){if(!game||state!=='playing')return;reorderOpen=!reorderOpen;UI.reorder.classList.toggle('hidden',!reorderOpen);mouse.down=false;if(reorderOpen)renderReorder()}

function updateHUD(){if(!game)return;const p=game.player,w=p.weapons[p.slot],d=weaponDefs[w.id];UI.wave.textContent=game.wave;UI.left.textContent=`${game.enemies.length+game.waveQueue} HOSTILES`;UI.score.textContent=Math.floor(game.score).toLocaleString();UI.hp.style.width=clamp(p.hp/p.maxHp*100,0,100)+'%';UI.hpText.textContent=`${Math.ceil(p.hp)} / ${p.maxHp}`;UI.weapon.textContent=d.name+(p.reloading>0?' · RELOADING':'');UI.ammo.textContent=String(w.ammo);UI.grenades.textContent=`Grenades: ${p.grenades}${game.power.double>0?' · 2X '+Math.ceil(game.power.double)+'s':''}${game.power.rapid>0?' · OVERDRIVE '+Math.ceil(game.power.rapid)+'s':''}`;updateWeaponSlots()}

const NAV=70;
function cellBlocked(map,cx,cy,r=12){const x=cx*NAV+NAV/2,y=cy*NAV+NAV/2;for(const o of map.obs)if(x>o.x-r&&x<o.x+o.w+r&&y>o.y-r&&y<o.y+o.h+r)return true;for(const b of map.barr)if(b.hp>0&&x>b.x-r&&x<b.x+b.w+r&&y>b.y-r&&y<b.y+b.h+r)return true;return false}
function pathfind(e,tx,ty){const m=game.map,cols=Math.ceil(m.size[0]/NAV),rows=Math.ceil(m.size[1]/NAV),sx=clamp(Math.floor(e.x/NAV),0,cols-1),sy=clamp(Math.floor(e.y/NAV),0,rows-1),gx=clamp(Math.floor(tx/NAV),0,cols-1),gy=clamp(Math.floor(ty/NAV),0,rows-1),key=(x,y)=>y*cols+x,start=key(sx,sy),goal=key(gx,gy),open=[{x:sx,y:sy,g:0,f:0}],came=new Map(),cost=new Map([[start,0]]),seen=new Set();let guard=0;while(open.length&&guard++<1800){open.sort((a,b)=>a.f-b.f);const cur=open.shift(),ck=key(cur.x,cur.y);if(seen.has(ck))continue;seen.add(ck);if(ck===goal){const pts=[];let k=ck;while(k!==start&&came.has(k)){const x=k%cols,y=Math.floor(k/cols);pts.push({x:x*NAV+NAV/2,y:y*NAV+NAV/2});k=came.get(k)}return pts.reverse()}for(const [dx,dy] of [[1,0],[-1,0],[0,1],[0,-1]]){const nx=cur.x+dx,ny=cur.y+dy;if(nx<0||ny<0||nx>=cols||ny>=rows||cellBlocked(m,nx,ny,e.r*.55))continue;const nk=key(nx,ny),ng=cur.g+1;if(ng<(cost.get(nk)??Infinity)){cost.set(nk,ng);came.set(nk,ck);open.push({x:nx,y:ny,g:ng,f:ng+Math.abs(nx-gx)+Math.abs(ny-gy)})}}}return[]}
function moveEnemy(e,dt,p){e.pathTimer-=dt;const directClear=!game.map.obs.some(o=>lineRect(e.x,e.y,p.x,p.y,{x:o.x-e.r*.6,y:o.y-e.r*.6,w:o.w+e.r*1.2,h:o.h+e.r*1.2}));let target=p;if(!directClear){if(e.pathTimer<=0||!e.path.length){e.path=pathfind(e,p.x,p.y);e.pathTimer=.45+Math.random()*.25}if(e.path.length){target=e.path[0];if(Math.hypot(e.x-target.x,e.y-target.y)<25)e.path.shift()}}const dx=target.x-e.x,dy=target.y-e.y,d=Math.hypot(dx,dy)||1;e.x+=dx/d*e.speed*dt;e.y+=dy/d*e.speed*dt}
function separateEnemies(){const es=game.enemies;for(let i=0;i<es.length;i++)for(let j=i+1;j<es.length;j++){const a=es[i],b=es[j],min=(a.r+b.r)*.42,dx=b.x-a.x,dy=b.y-a.y,d=Math.hypot(dx,dy);if(d>0&&d<min){const p=(min-d)*.5,nx=dx/d,ny=dy/d;a.x-=nx*p;a.y-=ny*p;b.x+=nx*p;b.y+=ny*p}}}

function update(dt){if((state!=='playing'&&state!=='between')||!game)return;const p=game.player,m=game.map;game.time+=dt;p.fireCd=Math.max(0,p.fireCd-dt);p.invuln=Math.max(0,p.invuln-dt);if(p.reloading>0){p.reloading-=dt;if(p.reloading<=0)finishReload()}game.power.double=Math.max(0,game.power.double-dt);game.power.rapid=Math.max(0,game.power.rapid-dt);
let dx=(keys[binds.right]?1:0)-(keys[binds.left]?1:0),dy=(keys[binds.down]?1:0)-(keys[binds.up]?1:0),l=Math.hypot(dx,dy)||1;dx/=l;dy/=l;const sp=p.speed*p.moveMult;p.x+=dx*sp*dt;for(const o of m.obs)resolveCircleRect(p,o);for(const b of m.barr)if(b.hp>0)resolveCircleRect(p,b);p.y+=dy*sp*dt;for(const o of m.obs)resolveCircleRect(p,o);for(const b of m.barr)if(b.hp>0)resolveCircleRect(p,b);p.x=clamp(p.x,p.r,m.size[0]-p.r);p.y=clamp(p.y,p.r,m.size[1]-p.r);
if(state==='playing'&&!reorderOpen&&mouse.down)shoot();
if(state==='playing'&&game.waveActive&&game.waveQueue>0){game.spawnTimer-=dt;if(game.spawnTimer<=0){spawnEnemy();game.waveQueue--;game.spawnTimer=Math.max(.18,.72-game.wave*.025)*rand(.7,1.2)}}
if(state==='playing'){for(const e of game.enemies){e.attackCd-=dt;e.hitFlash=Math.max(0,e.hitFlash-dt);let targetBarr=null;for(const b of m.barr)if(b.hp>0&&lineRect(e.x,e.y,p.x,p.y,b)){targetBarr=b;break}if(targetBarr){const tx=targetBarr.x+targetBarr.w/2,ty=targetBarr.y+targetBarr.h/2,dd=Math.hypot(tx-e.x,ty-e.y)||1;e.x+=(tx-e.x)/dd*e.speed*dt;e.y+=(ty-e.y)/dd*e.speed*dt;if(circleRect(e.x,e.y,e.r+4,targetBarr)&&e.attackCd<=0){targetBarr.hp=Math.max(0,targetBarr.hp-e.dmg*1.3);e.attackCd=.65}}else moveEnemy(e,dt,p);for(const o of m.obs)resolveCircleRect(e,o,e.r*.65);for(const b of m.barr)if(b.hp>0)resolveCircleRect(e,b,e.r*.65);if(Math.hypot(e.x-p.x,e.y-p.y)<e.r+p.r+3&&e.attackCd<=0){damagePlayer(e.dmg);e.attackCd=e.type==='runner'?.65:e.type==='elite'?.55:.82}}separateEnemies()}
for(let i=game.pickups.length-1;i>=0;i--){const pu=game.pickups[i];pu.life-=dt;pu.bob+=dt*3;if(pu.life<=0)game.pickups.splice(i,1);else if(Math.hypot(pu.x-p.x,pu.y-p.y)<p.r+pu.r+8)pickup(pu)}
for(let i=game.grenades.length-1;i>=0;i--){const g=game.grenades[i];g.t-=dt;g.x+=g.vx*dt;g.y+=g.vy*dt;g.vx*=Math.pow(.08,dt);g.vy*=Math.pow(.08,dt);for(const o of m.obs)if(circleRect(g.x,g.y,g.r,o)){g.vx*=-.35;g.vy*=-.35}if(g.t<=0){explode(g);game.grenades.splice(i,1)}}
for(let i=game.particles.length-1;i>=0;i--){const q=game.particles[i];q.life-=dt;q.x+=q.vx*dt;q.y+=q.vy*dt;q.vx*=Math.pow(.1,dt);q.vy*=Math.pow(.1,dt);if(q.life<=0)game.particles.splice(i,1)}game.bullets=game.bullets.filter(b=>(b.life-=dt)>0);game.corpses=game.corpses.filter(c=>(c.life-=dt)>0);
if(state==='playing'&&game.waveActive&&game.waveQueue===0&&game.enemies.length===0){game.waveActive=false;setTimeout(()=>{if(state==='playing')chooseUpgrades()},500)}
cam.x=clamp(lerp(cam.x,p.x-W/2,.13),0,Math.max(0,m.size[0]-W));cam.y=clamp(lerp(cam.y,p.y-H/2,.13),0,Math.max(0,m.size[1]-H));cam.shake=Math.max(0,cam.shake-20*dt);mouse.worldX=mouse.x+cam.x;mouse.worldY=mouse.y+cam.y;updateInteraction();updateHUD()}

function drawGrenadeShape(x,y,r,glow=false){ctx.save();ctx.translate(x,y);if(glow){ctx.shadowBlur=15;ctx.shadowColor='#ff765e'}ctx.fillStyle='#53604f';ctx.fillRect(-r*.55,-r*.7,r*1.1,r*1.4);ctx.fillStyle='#77836f';ctx.fillRect(-r*.38,-r*.88,r*.76,r*.25);ctx.fillStyle='#2d342b';ctx.fillRect(-r*.15,-r*1.05,r*.3,r*.22);ctx.strokeStyle='#aab1a4';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(r*.48,-r*.82,r*.32,-1.2,1.5);ctx.stroke();ctx.shadowBlur=0;ctx.restore()}
function drawPickup(pu){const y=pu.y+Math.sin(pu.bob)*4;if(pu.type==='grenade'){drawGrenadeShape(pu.x,y,pu.r,true);return}const colors={heal:'#59d98e',double:'#f2c14e',rapid:'#b16cff'};ctx.save();ctx.translate(pu.x,y);ctx.shadowBlur=16;ctx.shadowColor=colors[pu.type];ctx.fillStyle=colors[pu.type];ctx.beginPath();ctx.arc(0,0,pu.r,0,TAU);ctx.fill();ctx.shadowBlur=0;ctx.fillStyle='#09100d';ctx.font='900 10px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText({heal:'+',double:'2X',rapid:'>>'}[pu.type],0,1);ctx.restore()}
function draw(){ctx.clearRect(0,0,W,H);if(!game){drawMenuBG();return}const m=game.map,sh=cam.shake?rand(-cam.shake,cam.shake):0,sv=cam.shake?rand(-cam.shake,cam.shake):0;ctx.save();ctx.translate(-cam.x+sh,-cam.y+sv);ctx.fillStyle=m.floor;ctx.fillRect(0,0,m.size[0],m.size[1]);drawGrid(m);for(const c of game.corpses){ctx.globalAlpha=clamp(c.life/2,0,.30);ctx.fillStyle=c.color;ctx.beginPath();ctx.ellipse(c.x,c.y,c.r*1.2,c.r*.65,c.a,0,TAU);ctx.fill();ctx.globalAlpha=1}for(const o of m.obs)drawObstacle(o);for(const b of m.barr)drawBarr(b);for(const pu of game.pickups)drawPickup(pu);for(const g of game.grenades)drawGrenadeShape(g.x,g.y,g.r);for(const e of game.enemies)drawEnemy(e);drawPlayer(game.player);for(const b of game.bullets){ctx.globalAlpha=clamp(b.life/.055,0,1);ctx.strokeStyle=b.color;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(b.x1,b.y1);ctx.lineTo(b.x2,b.y2);ctx.stroke();ctx.globalAlpha=1}for(const q of game.particles){ctx.globalAlpha=clamp(q.life/q.max,0,1);ctx.fillStyle=q.color;ctx.fillRect(q.x-q.size/2,q.y-q.size/2,q.size,q.size);ctx.globalAlpha=1}ctx.restore();drawMinimap()}
function drawGrid(m){ctx.strokeStyle='rgba(255,255,255,.025)';for(let x=0;x<m.size[0];x+=80){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,m.size[1]);ctx.stroke()}for(let y=0;y<m.size[1];y+=80){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(m.size[0],y);ctx.stroke()}}
function drawObstacle(o){ctx.fillStyle=o.type==='building'?'#252b2e':o.type==='wall'?'#242a31':o.type==='lab'?'#2b242b':'#2c2924';ctx.fillRect(o.x,o.y,o.w,o.h);ctx.strokeStyle='rgba(255,255,255,.08)';ctx.lineWidth=3;ctx.strokeRect(o.x,o.y,o.w,o.h)}
function drawBarr(b){ctx.fillStyle=b.hp>0?'#654b35':'#342c27';ctx.fillRect(b.x,b.y,b.w,b.h);ctx.strokeStyle='#aa8056';ctx.strokeRect(b.x,b.y,b.w,b.h)}
function drawEnemy(e){ctx.save();ctx.translate(e.x,e.y);ctx.fillStyle=e.hitFlash>0?'#fff':e.color;ctx.beginPath();ctx.arc(0,0,e.r,0,TAU);ctx.fill();ctx.fillStyle='rgba(0,0,0,.24)';ctx.fillRect(-e.r*.6,-e.r*.35,e.r*1.2,e.r*.7);ctx.restore();if(e.type==='brute'||e.type==='elite'){ctx.fillStyle='rgba(0,0,0,.55)';ctx.fillRect(e.x-e.r,e.y-e.r-11,e.r*2,5);ctx.fillStyle=e.type==='elite'?'#d56cff':'#d96354';ctx.fillRect(e.x-e.r,e.y-e.r-11,e.r*2*(e.hp/e.maxHp),5)}}
function drawPlayer(p){const a=Math.atan2(mouse.worldY-p.y,mouse.worldX-p.x);ctx.save();ctx.translate(p.x,p.y);ctx.rotate(a);ctx.fillStyle='#dbe6ef';ctx.beginPath();ctx.arc(0,0,p.r,0,TAU);ctx.fill();ctx.fillStyle='#303a45';ctx.fillRect(6,-5,25,10);ctx.restore()}
function drawMinimap(){if(!game||state==='menu'||state==='gameover')return;const m=game.map,mw=150,mh=100,x=W-mw-18,y=90,sx=mw/m.size[0],sy=mh/m.size[1];ctx.fillStyle='rgba(0,0,0,.5)';ctx.fillRect(x,y,mw,mh);ctx.strokeStyle='rgba(255,255,255,.12)';ctx.strokeRect(x,y,mw,mh);ctx.fillStyle='#8b949f';for(const o of m.obs)ctx.fillRect(x+o.x*sx,y+o.y*sy,o.w*sx,o.h*sy);ctx.fillStyle='#ff6257';for(const e of game.enemies)ctx.fillRect(x+e.x*sx-1,y+e.y*sy-1,2,2);ctx.fillStyle='#55d7ff';ctx.beginPath();ctx.arc(x+game.player.x*sx,y+game.player.y*sy,3,0,TAU);ctx.fill()}
function drawMenuBG(){const t=performance.now()/1000;ctx.fillStyle='#07090c';ctx.fillRect(0,0,W,H);for(let i=0;i<44;i++){const x=(i*173+t*(2+i%5)*3)%W,y=(i*91+t*(1+i%3)*2)%H,a=.025+(i%4)*.008;ctx.fillStyle=`rgba(190,210,225,${a})`;ctx.fillRect(x,y,i%9===0?2:1,i%9===0?2:1)}}

function loop(now){const dt=Math.min(.033,(now-last)/1000);last=now;update(dt);draw();requestAnimationFrame(loop)}requestAnimationFrame(loop);

addEventListener('keydown',e=>{if(rebinding){e.preventDefault();const other=Object.keys(binds).find(a=>binds[a]===e.code&&a!==rebinding);if(other)binds[other]=binds[rebinding];binds[rebinding]=e.code;rebinding=null;saveBinds();renderControls();return}keys[e.code]=true;if(e.code==='Tab'){e.preventDefault();toggleReorder();return}if(e.code===binds.pause){e.preventDefault();if(state==='playing'){state='paused';UI.pause.classList.remove('hidden');showPausePane('main');mouse.down=false}else if(state==='paused'){state='playing';UI.pause.classList.add('hidden')}return}if(state!=='playing')return;if(e.code===binds.reload)reload();if(e.code===binds.grenade)throwGrenade();if(e.code===binds.interact)interact();const slots=[binds.slot1,binds.slot2,binds.slot3],i=slots.indexOf(e.code);if(i>=0&&game.player.weapons[i]){game.player.slot=i;game.player.reloading=0;updateHUD()}});
addEventListener('keyup',e=>keys[e.code]=false);addEventListener('mousemove',e=>{mouse.x=e.clientX;mouse.y=e.clientY;UI.cross.style.left=e.clientX+'px';UI.cross.style.top=e.clientY+'px'});addEventListener('mousedown',e=>{if(e.button===0&&!reorderOpen){mouse.down=true;ensureAudio()}});addEventListener('mouseup',e=>{if(e.button===0)mouse.down=false});addEventListener('contextmenu',e=>e.preventDefault());DSMobile.installBrowserGuards();

const startBtn=$('startBtn');window.DeadSectorStart=e=>{e?.preventDefault?.();startGame();return false};startBtn.onclick=window.DeadSectorStart;
$('howBtn').onclick=()=>{UI.menu.classList.add('hidden');UI.how.classList.remove('hidden')};$('backBtn').onclick=()=>{UI.how.classList.add('hidden');UI.menu.classList.remove('hidden')};$('resumeBtn').onclick=()=>{state='playing';UI.pause.classList.add('hidden')};$('restartBtn').onclick=()=>{UI.pause.classList.add('hidden');startGame()};$('quitBtn').onclick=()=>{state='menu';game=null;UI.pause.classList.add('hidden');UI.hud.style.display='none';UI.menu.classList.remove('hidden')};$('pauseControlsTab').onclick=()=>showPausePane('controls');$('openControlsBtn').onclick=()=>showPausePane('controls');$('controlsBackBtn').onclick=()=>showPausePane('main');$('resetControlsBtn').onclick=()=>{binds={...defaultBinds};saveBinds();rebinding=null;renderControls()};$('againBtn').onclick=startGame;$('menuBtn').onclick=()=>{state='menu';game=null;UI.gameover.classList.add('hidden');UI.menu.classList.remove('hidden')};
})();